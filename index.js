// arrays, objetos
let meta = {
    value: 'ler um livro por mês',
    checked: false,
    log: (info) => {
        console.log(info)
    }
}

meta.value = "não é mais ler um livro por mês"
meta.log(meta.value)

// Function   // Arrow function
const criarMeta = () => {}


